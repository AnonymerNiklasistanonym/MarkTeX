{{> navigation_bar}}


<div class="container-group">
	<div class="container-group-content">

		<h1>{{group.name}} from <a href="/account/{{group.owner.id}}">{{group.owner.name}}</a></h1>

		<br>

		<section class="section-group" id="section-group-members">
			<h2>Members:</h2>
			<ul>
				{{#each group.members}}
				<li><a class="button" href="/account/{{this.id}}">{{this.name}} ({{#if group.writeAccess}}[write]{{/if}}</a></li>
				{{/each}}
			</ul>
		</section>

		<section class="section-group" id="section-group-documents">
			<h2>Documents:</h2>
			<ul>
				{{#each group.documents}}
				<li><a class="button" href="/document/{{this.id}}">{{this.title}} by {{this.authors}} ({{this.date}})</a></li>
				{{/each}}
			</ul>
		</section>

	</div>
</div>
