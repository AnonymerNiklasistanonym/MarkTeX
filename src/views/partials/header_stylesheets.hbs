{{!--
--------------------------------------------------------------------------------
Header stylesheets partial
--------------------------------------------------------------------------------
Parameters:
{
  header: {
    stylesheets: { path: string }[] // CSS paths
  }
}
--------------------------------------------------------------------------------
--}}

{{#each header.stylesheets}}
<link href="{{this.path}}" type="text/css" rel="stylesheet" />
{{/each}}
