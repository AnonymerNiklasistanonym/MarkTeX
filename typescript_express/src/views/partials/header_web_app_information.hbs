{{!--
--------------------------------------------------------------------------------
Header web app information partial
--------------------------------------------------------------------------------
Parameters:
{
  header: {
    webApp: {
      name: string, // The name of the web app
      themeColor: string, // HEX color of the web app (chrome specific)
      manifestPath: string // Path to web app manifest
    }
  }
}
--------------------------------------------------------------------------------
--}}


{{!-- Special Chrome tags --}}
<meta name="application-name" content="{{header.webApp.name}}" />
<meta name="theme-color" content="{{header.webApp.themeColor}}" />

{{!-- Web app manifest --}}
<link rel="manifest" href="{{header.webApp.manifestPath}}">
